# ArchWise 改进清单

> 工作文档。用途：记录已讨论、已确认的改进方向，作为后续动手的依据。
> 当前阶段＝**只记录方向，尚未实施**。视角：工程实现与项目打磨。
> 每条方向标注状态：`已认可` / `倾向采纳，待最终确认` / `待讨论` / `已实施`。

---

## 待改进的四个问题（已锁定）

1. **知识进化没做到位**：`add_style` 能用（新增风格会并入候选并喂给大模型）；但 `add_case` 只写 `learned_cases.json`、推荐流程从不读取 → 案例学习无闭环；图谱进化的机制在但当前处于停用/休眠。
2. **微服务架构没做**：单体 FastAPI，内部按 `services / agents / knowledge` 分层，但未拆分。
3. **实际没有 ≥3 类独立智能体**：真正作为独立 Agent 类跑在流程里的只有 `EvaluationGeneratorAgent`，且默认的流式路径连它都绕过；`ArchitectureMatcherAgent` 是死代码；“需求解析 Agent”只是 `llm_client.extract_features` 一个方法。
4. **混合推理被停用**：`hybrid_orchestrator.py:691` 显式停用规则引擎，纯大模型决策；`decision_trace.rule_evidence.enabled=False`。

**根因（#3 与 #4 共因）= 本地推理层是死代码**：`matcher.match()`、`rule_engine.evaluate / validate_candidates()`、`composition_recommender.*`、`retrieve_styles()` 在整个 `app/` 里零调用——被实例化但从不执行，决策全部交给大模型。`data/rules.json` 的 6 条规则因此白写。

---

## 方向一：推理决策“换角色”（主要覆盖 #4，并让 #1 真正有意义）

**状态：`已认可`，待细化、未实施。**

### 背景：协作者当初为什么切到纯大模型（两个真实约束，必须保留其收益）

- **速度**：旧混合流程每次推荐要两三分钟。
  诊断：根因不是图查询（图查询是毫秒级），而是**一连串串行的远端调用**——多次大模型调用（如拓扑段的漏项复核 + 多轮补全，每轮十几秒超时）叠加现场串行的图/向量调用。
  关键事实：本地规则引擎与本地匹配器是**内存内运算，微秒级**，几乎不耗时。慢的是远端调用链，不是“规则”或“本地打分”。
- **泛化**：旧混合对库里存过的需求结果不错，库里没有的新需求结果很差。
  诊断：旧设计把**知识库放在了决策质量的关键路径**上，且靠脆弱的字面匹配；库没覆盖到的新需求匹配分接近零，退回弱默认值，结果就差。大模型自带先验、泛化好，这是纯大模型在新需求上更好的原因。

### 目标设计：重新分配各部件的角色

- **大模型＝主力**：负责理解需求、生成候选、生成报告。对新需求泛化好、速度可接受。保留。
- **规则引擎＝事后校验**（不再做事前筛选）：大模型先出候选，规则引擎用硬约束检查这批候选是否违规、是否需要重排。复用现成的 `validate_candidates`。纯内存、几乎零耗时；只在明显违规时动手，**不损害大模型对新需求的发挥**。仅此一步，“混合推理”即以接近零成本恢复。
- **知识库（图谱 / 案例）＝可选补充信息**（不做决定性来源）：命中相关知识就作为额外材料喂给大模型增强；未命中（真正的新需求）就**不附加任何东西**，让大模型照常发挥。知识只做加法 → 只会帮忙、不会拖后腿，旧设计“库里没有就变差”的问题消失。
- **进化 / 写入＝后台执行**：学习新知识不占用本次请求耗时。
- **缓存**：相同或高度相似的需求可快速返回。

### 待细化的子问题（动手前要先定）

- 何时去查知识库；查到的知识用什么形式注入大模型；查不到时的行为。
- 规则事后校验的具体触发条件与重排逻辑（沿用还是调整现有 `validate_candidates`）。
- 缓存键如何设计（基于什么判定“相同 / 相似”）。
- 把“需求解析 / 架构匹配 / 评估生成”整理成清晰的独立 Agent 边界（与 #3 联动）。

---

## 方向二：知识存储层规范化（覆盖 #1 的“存储”面）

**状态：`已认可`，分两期实施。**

原则：每块知识按其**数据形状**和**查询方式**选存储，不混用；JSON/代码降级为只读种子，运行时不再被逻辑直接读取。

| 知识块 | 目标存储 | 理由 |
| --- | --- | --- |
| A 架构风格库 | **Neo4j** | 风格—类别—场景—质量属性是关系型知识，构成风格知识图谱；数据量小，不需要向量 |
| B 领域拓扑知识 | **Neo4j（结构 + 原生向量索引）** | 结构（场景→能力→组件→存储→依赖）天然是图；节点语义向量用 Neo4j 原生向量索引，与结构**同库** → 无跨库双写 |
| C 案例库 | **Chroma** | “给新需求找最相似的历史需求”是文本相似度检索的标准场景；案例无关系结构，不进 Neo4j |
| 规则（6 条） | 配置 / 代码 | 是决策逻辑而非知识数据，不进任何库 |
| 初始种子数据 | JSON / 代码（只读） | 可追溯、可版本管理；启动时幂等导入数据库，运行时不再被直接读取 |

### 已定的设计决定

- **B 向量并入 Neo4j**（已确认）：用 Neo4j 原生向量索引 + LangChain `Neo4jVector`，B 的结构与向量同处一库 → **消除双写一致性问题**。Chroma 只留给 C。
- **embedding＝通义 text-embedding-v4**（DashScope，OpenAI 兼容，已确认）：现有 `embed_texts` 已按 Qwen text-embedding-v4 风格写（每批 ≤10 条），接入改动最小；落现有 `EMBEDDING_*` 配置位，经 LangChain Embeddings 包装。
- **数据模型**：Neo4j 复用现有 A/B schema；Chroma 案例集合**嵌入“抽象后的需求特征”**（领域/质量属性/数据流），元数据＝可信状态(trusted/candidate)、推荐架构、置信度、来源(种子/运行时)、时间戳。
- **种子与导入**：`styles.py` / `domain_topology.json` / `test_cases.json` 作只读种子，写**幂等导入脚本**，初始化时灌库（重复跑不重复插）。
- **一致性模型**：Neo4j＝A+B 唯一真源；Chroma＝C 主存；B 不跨库。
- **访问层**：统一走 LangChain —— `Neo4jGraph`(A+B 图查询) / `Neo4jVector`(B 语义检索与去重) / `Chroma`(C 案例)。

### 分期（动手面压到最小）

- **第一期（解锁 基础#1）**：只建 **Chroma + 通义 embedding + 案例集合**，不需要 Neo4j。
- **第二期（做 B 拓扑进化、迁移 A 进图库时）**：搭 **Neo4j**，导入 A+B，接 `Neo4jGraph`/`Neo4jVector`。Neo4j 部署形态（云上 / 本机）第二期再定。

---

## #1 知识进化（原方向三）

**状态：`已认可`，分两期实施。本期范围只做案例库（C）；拓扑库（B）进化暂不做、风格库（A）维持现状。**

三个知识面的现状（保留作背景）：
- **风格库（A）**：唯一闭着的环，但只是人工增删，没有“从使用中学习”的信号。
- **拓扑库（B）**：读路径（生成器读 `domain_topology.json`）与写/进化路径（大模型补丁 → 规范化 → 写库）不连通，学到的知识喂不回读路径。**本期不做。**
- **案例库（C）**：只写不读，完全开环，缺口最大也最干净。**本期就做它。**

> 与方向一/二的关系：知识作“可选补充”（方向一）后进化才有意义；存储依赖方向二的 Chroma + embedding 先就位。

### 基础 #1（先做）——人工策展 + 自动捕获后确认才信任

做法一与做法二结合：

1. **专家策展可信库（做法一）**：维护一批人工校验过的可信案例，用 `data/test_cases.json` 的 20 条作冷启动种子；通过 API/界面增删；运行时**不无条件自动学**。
2. **自动捕获 + 确认才信任（做法二）**：每次推荐先落为“候选案例”，拿到确认信号（用户采纳 / 策展审核通过）后才升级为“可信案例”，**只有可信案例允许被检索注入**。
3. **检索注入闭环**：新需求 → 按相似度从可信库召回 Top-K（带阈值，低于阈值不注入）→ 作为参考注入架构匹配环节（**只参考不裁决**，保泛化）→ 在决策溯源写明“参考了 N 条历史相似案例（相似度 …）”。
4. **embedding：用国内平台的 OpenAI 兼容 embedding API**（落在现有 `EMBEDDING_*` 配置位；代码 `embed_texts` 已按 Qwen text-embedding-v4 风格设计，如通义 DashScope / 硅基流动 bge 之类），经 LangChain Embeddings 包装，向量入 **Chroma**。

### 进阶 #1（基础版之后再做）——文献加持版

研究依据：自进化 agent / agent 记忆的 **8 篇顶会主会**（详见项目笔记 agent-memory-literature）——Reflexion(NeurIPS'23)、Self-Refine(NeurIPS'23)、ExpeL(AAAI'24)、MemoryBank(AAAI'24)、Synapse(ICLR'24)、Agent-Pro(ACL'24)、Agent Workflow Memory(ICML'25)、A-MEM(NeurIPS'25)。共同范式：行动→自评/反思→蒸馏成结构化可泛化记忆→相似任务检索注入→记忆随时间精炼，**全程非参数（不训练模型）**。

在基础版上叠加这些升级：

1. **两层记忆**：案例层（情节，抽象特征→推荐+结果，做少样本范例）+ 洞见层（语义，跨多条案例蒸馏出的可泛化规则，做指导原则）。— ExpeL / AWM
2. **失败也学**：用户否决/纠正的推荐 → 沉淀“避免”类洞见，不只存正例。— Reflexion / ExpeL
3. **检索用抽象特征而非原文**：拿结构化需求特征（领域/质量属性/数据流）做向量匹配，更稳。— Synapse
4. **质量闸口用自我批判**：一条经验先自评/精炼再升级“可信”。— Self-Refine
5. **遗忘曲线 + 笔记链接演化**：按命中频次/时间加权衰减陈旧案例；新证据回头修订相关旧洞见、建立链接。— MemoryBank / A-MEM
6. 定位为策略级而非单步（更新“判断这类需求的方式”）。— Agent-Pro

---

## #2 微服务架构 → 服务划分（模块化单体为主，物理上抽强边界）

**状态：`已认可`，待实施。**

立场：完整微服务对本项目是过度设计（额外的网络延迟、跨服务数据一致性、多进程运维、部分失败处理，而当前没有伸缩/多团队/独立伸缩的驱动）。但作业考核的是“微服务**划分**合理性”——考的是划分质量，不是必须跑起 N 个进程。因此采取“完整划分写进设计 + 物理上只拆强边界”的策略。

### 拆分依据（一个边界至少占一条才值得独立成服务）
1. 围绕一块完整业务能力，有清晰输入输出和接口契约；
2. 拥有自己的数据（数据自治，最硬的边界）；
3. 有独立的运行特征（负载/耗时/伸缩/变更频率明显不同）。

### 目标服务划分（写进设计文档的完整划分）

| 服务 | 对应现有代码 | 职责 | 拥有数据 | 依赖 |
| --- | --- | --- | --- | --- |
| API 网关 / 接入服务 | `main.py` 路由 + `templates/` + `static/` + 编排器的协调部分 | 收请求、调度各服务、汇总、SSE 推流、托管前端 | 无（仅请求态） | 下面各服务 |
| 需求理解服务 | `llm_client.extract_features` + Schema 校验/归一 | 自然语言 → 结构化特征 | 无 | LLM 网关 |
| 推理决策服务 | `ArchitectureMatcherAgent` + `RuleEngine`+`rules.json` + `recommend_architectures` + 组合推荐 | 特征+知识 → 候选+评分+组合；规则做硬约束校验（混合推理/多智能体落地处） | 规则（配置） | 知识服务、LLM 网关 |
| 评估报告服务 | `EvaluationGeneratorAgent` + `report_formatter` + `generate_report/stream_report` | 候选 → 可读评估报告（支持流式） | 无 | LLM 网关 |
| 拓扑生成服务 | `topology_generator` + 拓扑相关 LLM 调用 + 覆盖率/补全循环 | 需求+特征+推荐 → 定制拓扑图 | 无（可加缓存） | 知识服务、LLM 网关 |
| 知识服务 | `repository` + `knowledge_graph` + `neo4j_aura` + `knowledge_normalizer` | 统一管理风格/拓扑图谱/案例，提供读知识与写知识（进化后台执行） | **唯一拥有持久数据**：Neo4j + 向量库 | Neo4j、向量库 |
| LLM 网关服务 | `llm_client`（聊天+embedding+超时/重试） | 调大模型/embedding 的唯一出口，集中密钥/重试/限流/模型选择/缓存 | 无（可有缓存存储） | 外部大模型/embedding |

### 诚实评估：哪些边界硬、哪些凑数
- **硬边界（值得真拆）**：知识服务（唯一拥有持久数据）、LLM 网关（横切基础设施、被多服务共用、现有代码已隔离在 `llm_client.py`、最易抽出）、拓扑生成服务（最重、耗时特征特殊，可独立伸缩/缓存）。
- **凑数边界（不值得真拆）**：需求理解 / 推理决策 / 评估报告是一次请求里前后相连、且无持久数据的纯计算步骤；拆开只会在每步之间加网络往返，换不来独立伸缩好处。→ 应作为同一服务内部的模块。
- **关键观察**：本系统绝大多数环节是无状态计算步骤，真正拥有数据的只有知识服务一个。所以最有意义的边界是“知识服务”和“LLM 网关”两条。

### 落地取舍（推荐）
- **划分（设计文档）**：用上面完整的 7 服务图，作为“划分合理性”的成果物。
- **物理拆分**：只把两条最硬边界做成真实独立服务——**LLM 网关服务** + **知识服务**；其余“需求理解+决策+报告”合成一个**推理编排服务**、内部分模块；拓扑生成视情况决定是否单独抽。其余保持模块化单体。

## #3 ≥3 类独立智能体（框架优先重建）

**状态：`已认可`，与 #4 同刀解决、与 #1 联动。**

技术路线（用户拍板）：**框架优先，自研最小化。**
- **LangChain / LangGraph 直接用**，能用框架的尽量用：
  - LLM 调用 → LangChain chain：`prompt | llm.with_structured_output(Schema)`，替掉手写的 `_extract_json` / `_sanitize_*` / strict 重试。
  - 编排 → LangGraph `StateGraph`（节点=agent，状态=`ReasoningContext`），替掉自研 `HybridReasoningOrchestrator` 的 `run`/`stream` 两套。
  - 拓扑补全循环 → LangGraph 条件边（循环回补全节点）。
  - LLM 缓存 → LangChain `set_llm_cache`（顺带补上缺失的缓存项）。
  - 向量库/图谱 → LangChain 的 Chroma / `Neo4jVector` / `Neo4jGraph` 集成（对接方向二）。
- **对 AstrBot / Hermes / OpenClaw：借模式与小规格，不抠成块代码。** 理由：三者是聊天助手框架，代码与各自框架（消息事件/管线/插件/会话/通道）深耦合，解耦成本常高于自写那一小块，且会引入不可控行为——正是“没预见的坑”。可直接借的是小规格：Hermes 的案例/技能**文档结构**（frontmatter + 步骤/坑/验证 + 渐进式披露）→ 喂给 #1。重机械（agent 自驱循环、有状态编排）LangGraph 已提供，无需去抠。
- **“造轮子”边界**：用 LangChain/LangGraph/Chroma = 用轮子；写我们自己的 Agent 类+提示词+把 `RuleEngine` 接进匹配 Agent = 不可避免的应用逻辑，不算造轮子（没有现成的“架构推荐 Agent”可下载）。自研只留这部分。

目标结构：`Agent` 抽象（统一 `run(ctx)->ctx`）+ 具体 agent（RequirementAnalysisAgent / ArchitectureMatchingAgent〔内接 RuleEngine+本地匹配器 → 同时修 #4〕 / EvaluationAgent，可选 TopologyAgent、KnowledgeCurationAgent〔真 agent 循环〕）+ AgentCoordinator（=LangGraph 图，真实写 `decision_trace`，matcher 与 rule-validator 构成“生成者+评审者”协作）。

低风险接入顺序（避免大爆炸重写）：① LLM 调用逐个换 LangChain chain（结构化输出+缓存）→ ② 编排换 LangGraph、匹配节点接规则引擎（#3+#4 同步落地）→ ③ Chroma 案例检索（#1）→ ④ Neo4j 图谱（方向二）。
集成摩擦预算：DeepSeek 专有参数（`thinking`/`response_format`）走 `model_kwargs`/`extra_body`；流式 token 聚合迁到框架；锁 LangChain 各包版本。

---

## 未来方向（暂缓，工程量较大，先记录不实施）

### F1 · Agent 协作日志升级为"真实运行树"
现状：协作日志是各 LangGraph 节点里手写的 `ctx.trace.append(...)` 字符串——属于"叙述"而非"仪表化"，可能与实际执行脱钩。
目标：改成**事件驱动的运行树**——
- 用 LangGraph `astream_events(version="v2")` 或挂 `BaseCallbackHandler`（附加式，不改现有 SSE）捕获每个真实跑过的节点（`run_id/parent_run_id/时间/输入输出`），按父子关系拼成 span 树。
- 每个节点配一个**描述函数**（从真实输出生成可读标签，确定性、不用 LLM），并按 role（agent/规则引擎/模块）上色分类。
- 前端做一个可折叠缩进树（类 LangSmith 左侧树），节点带耗时小条 + 可展开输入输出。
- 接入点已勘察：`hybrid_orchestrator.py` 两处 `compiled.ainvoke(...)`（推荐图 837、拓扑图 441）传 `config={"callbacks":[collector]}`；`ReasoningContext` 是普通 dataclass 可挂 `agent_run_tree`；`_build_decision_trace`(1108) 处注入。

### F2 · LangSmith 外部可观测（可选）
设 `LANGCHAIN_TRACING_V2=true` + `LANGCHAIN_API_KEY` + `LANGCHAIN_PROJECT` 即自动把每次图执行上报成调用树。零代码改动，但是外部托管 + 独立网站 + 数据出本机；定位为开发期 debug 工具，不替代 in-app 运行树（F1）。

---

## 变更记录

- 2026-06-04：建立本清单；记录方向一（推理决策换角色，已认可）、方向二（知识存储规范化，倾向采纳）、方向三（知识进化闭环，待定切入点）。尚未改动任何代码。
- 2026-06-04：#2 微服务确定为“完整服务划分（7 服务）+ 物理只拆 LLM 网关与知识服务、其余模块化”，状态改为已认可。仍未改动代码。
- 2026-06-04：#3 确定技术路线为“框架优先：LangChain + LangGraph 直接用；三个聊天框架借模式/小规格不搬成块代码；自研只留 Agent 业务逻辑”，与 #4 同刀解决。状态改为已认可。仍未改动代码。
- 2026-06-04：#1（原方向三）定案——本期只做案例库 C，分两期：基础版＝人工策展可信库(20 条测试案例作种子)＋自动捕获后确认才信任＋相似度检索注入(只参考不裁决)，embedding 用国内 OpenAI 兼容 API 入 Chroma；进阶版＝文献加持(8 篇顶会主会)的两层记忆/失败也学/抽象检索/自评闸口/遗忘演化。状态改为已认可。仍未改动代码。
- 2026-06-04：方向二定案——B 向量并入 Neo4j 原生向量索引(消除双写)、Chroma 只留 C；embedding 用通义 text-embedding-v4(DashScope)；分两期(第一期只建 Chroma+embedding 解锁基础#1，第二期才上 Neo4j)。状态改为已认可。仍未改动代码。
- 2026-06-05：#1 基础版（案例 create-or-refine 去重 + 人工增删/提升可信 + 检索注入）已实现并验证；前端知识中心已上线。运行树(F1) + LangSmith(F2) 因工程量较大转入"未来方向"暂缓。协作者(gpt-5.5)已把项目改成四个真实独立微服务（网关 8010 / 推理 8011 / 知识 8012 / LLM 网关 8013，同套代码靠环境变量切换单体/微服务）。下一步：更新全部 docs + 做答辩 PPT。
